name: sample_sambhavam
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  chewie: ^1.10.0
  flutter:
    sdk: flutter
  logger: ^2.5.0  # Add this line
  lottie: ^2.6.0
  backendless_sdk: ^8.0.0-alpha.33
  google_fonts: ^6.2.1
  video_player: ^2.9.3
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  cloud_firestore: ^5.6.5
  firebase_core: ^3.12.1  # ✅ Make sure firebase_core is updated
  firebase_auth: ^5.5.1 # ✅ Install the latest Firebase Authentication
  http: ^1.3.0
  crypto: ^3.0.3
  shared_preferences: ^2.5.2
  googleapis: ^14.0.0
  googleapis_auth: ^1.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  intl: ^0.20.2  # For date formatting
  flutter_animate: ^4.3.0
  flutter_lints: ^5.0.0

  geolocator: ^10.1.0
  

flutter:
  uses-material-design: true

  assets:
    - lib/images/img4.jpg
    - lib/images/img1.jpg
    - lib/images/RecHere.mp4
    - lib/images/land.jpg

